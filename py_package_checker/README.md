# Py Package Checker

A small python script to generate a python file with only imports to verify that all python files in a package can be imported by an external program.

## Usage

`python3 py_package_checker.py <package_path> <output_file>`